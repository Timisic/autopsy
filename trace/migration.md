# 2026-09-24 仓库清理记录

## 输入

依据对话中已交付的 `autopsy_writing_handoff_final.zip`，将 `final_en/`、`final_zh/` 的成稿整理为 `en/`、`zh/`。基线仓库提交为 `51e1e780111c712e43e1202e2b9f305111f3eaf3`。

## 保留、迁移与移除

保留六份成稿 PDF、对应 LaTeX、两套表格和必要图形。统一根目录编译入口，删除两个语言目录重复的 README 和 build.sh。正文源码中的段落用途注释移入追溯记录，可见内容不变。

原配置、研究输入与计划、文献证据、分析代码、汇总结果和核验记录迁入 `trace/baseline/`，保持原始字节。旧 `manuscript/`、旧 `figures/`、已失效的根目录导航和旧仓库工具测试不留在当前树中；它们仍在基线提交中。

本次未修改统计量、模型选择、作者声明或学术论证。未上传逐人数据、第三方全文、字体、截图、压缩包和编译缓存。未强制推送或改写旧历史。

## 验证范围

检查必要资源完整、两种语言表格数字一致、LaTeX 注释清理不改变可见文本，以及六份 PDF 的编译、页数与可读性。远程构建结果与冻结结果哈希写入 `repository_checks.json`；当前文件清单写入 `publication.json`。原写作核验快照仅表示其原执行范围。
